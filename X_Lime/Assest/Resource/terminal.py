import subprocess
import os
def terminal_resize1():
    subprocess.call(['/usr/bin/resize', '-s', '17', '48'], stdout=open(os.devnull, 'wb'))
pass
def terminal_resize2():
    subprocess.call(['/usr/bin/resize', '-s', '9', '78'], stdout=open(os.devnull, 'wb'))
pass
def terminal_resize3():
    subprocess.call(['/usr/bin/resize', '-s', '23', '53'], stdout=open(os.devnull, 'wb'))
pass
def terminal_resize4():
    subprocess.call(['/usr/bin/resize', '-s', '21', '51'], stdout=open(os.devnull, 'wb'))
pass
def terminal_resize5():
    subprocess.call(['/usr/bin/resize', '-s', '14', '34'], stdout=open(os.devnull, 'wb'))  
    pass
def terminal_resize6():
    subprocess.call(['/usr/bin/resize', '-s', '27', '93'], stdout=open(os.devnull, 'wb'))
    pass
def terminal_resize7():
    subprocess.call(['/usr/bin/resize', '-s', '18', '49'], stdout=open(os.devnull, 'wb'))
    pass
def terminal_resize8():
    subprocess.call(['/usr/bin/resize', '-s', '14', '38'], stdout=open(os.devnull, 'wb'))
    pass
def terminal_resize9():
    subprocess.call(['/usr/bin/resize', '-s', '20', '30'], stdout=open(os.devnull, 'wb'))
    pass
def terminal_resize10():
    subprocess.call(['/usr/bin/resize', '-s', '20', '53'], stdout=open(os.devnull, 'wb'))
    pass
def terminal_return():
    subprocess.call(['/usr/bin/resize', '-s', '24', '80'], stdout=open(os.devnull, 'wb'))
    pass